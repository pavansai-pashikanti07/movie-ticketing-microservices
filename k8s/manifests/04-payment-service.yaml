apiVersion: v1
kind: ServiceAccount
metadata:
  name: payment-service-sa
  namespace: cinepass-dev
  labels:
    app: payment-service
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::304960798044:role/cinepass-dev-app-services-role
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: cinepass-dev
  labels:
    app: payment-service
    app.kubernetes.io/part-of: cinepass
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      serviceAccountName: payment-service-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: payment-service
          image: 304960798044.dkr.ecr.ap-south-2.amazonaws.com/cinepass-payment-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
              name: http
          # Ingest shared environment from centralized ConfigMap
          envFrom:
            - configMapRef:
                name: cinepass-config
          # Ingest sensitive database credentials from Kubernetes Secret
          env:
            - name: PORT
              value: "8083"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cinepass-credentials
                  key: DB_PASSWORD
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: cinepass-dev
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
  selector:
    app: payment-service
